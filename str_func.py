def func_title(value):
    """returns a string with all capital letters"""
    return value.title()


def new_func(value):
    """capitalize the first letters of each word in the line"""
    return value.upper()
